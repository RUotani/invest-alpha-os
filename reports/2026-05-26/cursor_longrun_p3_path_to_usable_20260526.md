# Cursor longrun wave 6 — p3_path_to_usable

## PR

Unified `p3_path_to_usable` in forward-p3-status: path A (horizon) + path B (ISO week writes) + gaps + next_steps.

Horizon timeline gains `sessions_until_histogram`.
