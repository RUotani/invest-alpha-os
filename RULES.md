# RULES.md — invest-alpha-os Project Rules

このファイルは AI アシスタント(ChatGPT / Claude / Claude Code)が **毎タスク開始時に必ず読む** ためのルール集です。提案ではなく**命令文**として扱ってください。

ユーザーがこのファイルを参照したら、以下のすべてのルールに照らして応答を検査してから出力すること。違反の可能性がある場合は出力前に停止し、ユーザーに確認を求めること。

最終更新: 2026-05-29

---

## §0. このファイルの運用ルール

- 各タスクの最初に必ず本ファイル全体を読むこと。
- 「読みました」と明示的に宣言してからタスクに着手すること。
- 同じ説明を2回ユーザーから受けたルールは、必ず本ファイルに追記提案すること。
- 本ファイルとユーザー指示が矛盾する場合、ユーザー指示を優先するが、矛盾を明示的に指摘すること。
- 本ファイルの改訂はユーザー承認なしに行わない。

---

## §1. 開発作業の絶対禁止事項

以下は確認なしに**絶対に実行しない**。実行を提案することも禁止。

- `git push origin main` などの main 直 push
- `git push --force` などの force push
- `git branch -D` などの branch 強制削除
- `git worktree remove` などの worktree 削除
- live HTTP リクエスト(キャッシュ書き込みを伴うもの)
- キャッシュへの新規書き込み
- Gmail 送信
- secrets / `.env` / token のログ出力・標準出力・コミット
- trading recommendation 関連コードの変更
- daily / signals の default behavior 変更
- `pyproject.toml` / `Makefile` / `.github/workflows/*` の変更
- GitHub auto-merge 設定

許容されるのは: read-only audit、dry-run、tests、docs、gated execution、PR作成、CI確認、evidence生成、人間承認後の squash merge。

---

## §2. コード品質の絶対原則

- **銘柄コード正規化**: 4桁数字限定の正規表現は禁止。日本株の英字含む5桁コード(例: Kioxia の `285A`)を必ず通すこと。テストケースに `285A` を最低1件含めること。
- 新規関数には type hints を必須。
- 失敗系のテストを最低1ケース含めること(正常系のみのテストは PR レビューで rejection 対象)。
- マジックナンバーは定数化、または config ファイルへ。
- print デバッグ残置禁止。logging を使うこと。

---

## §3. ChatGPT への作業依頼の鉄則

ユーザーが ChatGPT に作業を依頼するときは、ChatGPT は以下を遵守する。

- 「手順を列挙せよ」と言われた場合でも、**実行可能な Bash スクリプト / Python スクリプト** として出力すること。コマンドの羅列で済ませない。
- 結論を最初に、根拠と確度(%)をその後に提示すること。
- すべての分析に **反証セクション**(楽観バイアス排除)を含めること。
- 不明点は推測せず、`[要確認]` ラベルを付けて明示すること。
- 「これを覚えておいて」とユーザーが言った場合、Memory に明示的に保存し、保存内容を提示して確認を求めること。

---

## §4. 自律開発の制約

- 1つの PR は1つの **Product theme / 投資判断上のボトルネック解消単位** とする。危険な巨大PRは避けるが、ユーザー価値に直結する関連変更は、**単一ロングラン内でまとめて進めてよい**。
- 同一ロングラン内で複数PRを連続処理してよい（調査・実装・テスト・CI・squash merge・main基準再生成・reports-private redacted sync・approval package 準備まで）。**作業を細切れに止めて都度ユーザー確認を求めない**。
- **同一repoでの並行実装PRは禁止**。読み取り専用の調査Agentのみ並行可。並行禁止は「細切れ停止」を意味しない。
- 同一セッションで `signals/` と `operator/` を同時に触らない。
- PR は人間承認後に squash merge。自動 merge 禁止。
- consolidation PR を作成する場合、含まれる個別 PR のすべてを diff レベルで列挙すること。
- CI green は merge の必要条件であって十分条件ではない。CI green でもレビュー無しの merge は禁止。

---

## §5. プロジェクト優先順位の絶対原則

- **最優先は投資判断AIの精度改善に直結するボトルネック解消**（データ鮮度、候補生成精度、validation、context pack品質、実運用判断）。周辺整備・レポート装飾・追加ダッシュボードより優先する。
- **投資ロジック(`signals/`)が Ops 基盤(`operator/`)より常に優先**。
- 過去30日で `operator/` への変更が `signals/` への変更より多い場合、警告フラグを立てること。
- "完成度" の定義は **「投資シグナルが本番観察モードで稼働しているか」** であって、CLI機能数ではない。
- 以下の3ファイルが未実装の限り、`operator/` への機能追加は原則禁止:
  - `signals/momentum.py` のシグナル検出
  - `signals/peer_sync.py` のシグナル検出
  - `signals/veto_rules.py` のシグナル検出

---

## §6. Architecture Astronaut の検知と回避

以下のパターンを検知したら**即座に停止し、ユーザーに警告**すること。

- 連続する Ops 系 PR(operator-runner、CI、scaffold)が3本以上続いたとき
- 「12時間枠を埋める」「キューを盛る」など、**作業時間を埋めることを目的化**した発想が出てきたとき
- 「自動化を自動化する」階層が3層以上になったとき
- 投資ロジック実装より先に「実装を管理する仕組み」を作ろうとしたとき

警告メッセージは「§6 違反の可能性: [具体的内容]。投資ロジックの実装を優先しますか?」の形式で。

---

## §7. 投資判断の絶対原則

- **NISA は売却対象外**。すべての売却/縮小判断は特定口座のみが対象。
- **AI 集中ルール**:
  - narrow AI ポジションは株式資産の50%以下を維持
  - NDX -15% → 新規 AI 買い停止
  - NDX -25% → 積立含む全 AI 追加停止
  - NDX -50% かつ 18か月以上持続 → 撤退検討
- **エントリー規律**: すべての新規ポジションは打診 / 本命 / 深押しの3トランシェに分割。一括買い禁止。
- すべての推奨に **価格帯**(打診/本命/深押し)を必ず付与すること。
- 高 PER 回避ルールは AI 関連株について明示的に停止済み(2026年5月改訂)。これを忘れないこと。

---

## §8. 投資分析の出力形式

- 結論を最初に、確度(%)付きで提示。
- 反証セクション(bear case / disconfirming evidence)を必須で含める。
- 「現在不足している情報」「これがあれば確度が上がる情報」を明示する。
- 単独銘柄の分析でも、比較可能な peer / 代替案を最低1つ提示。
- 短期 / 中期 / 長期の時間軸を分けて記述。
- 最新データに依存する判定は、必ず再確認時刻を明示。

---

## §9. Chat / セッション管理

- 30分以上の議論で確立した戦略判断は、`docs/decisions/YYYY-MM-DD_*.md` にコミットしてから新 Chat へ移行すること。
- 新 Chat 開始時は本 RULES.md と関連 decisions ファイルを最初に読むこと。
- 「前のチャットで決めた」という発言は事実確認なしに信用しないこと。decisions ファイルで verify すること。

---

## §10. 違反検知時のプロトコル

ユーザーが「RULES.md §N 違反」を指摘した場合:

1. 即座に作業を停止
2. 違反した条項を引用
3. 違反した具体的内容を1行で説明
4. 再発防止策を1行で提示
5. 続行可否はユーザーが判断するまで待機

---

## §12. エージェント長時間 run の運用

- 人間の作業を最小化するため、エージェントによる長時間連続実行(long-run)は積極的に活用する。
- ただし以下の前提条件を**すべて**満たす場合のみ:
  - キュー内の作業の **60%以上が §5 で定義された投資ロジック(`signals/` / `risk/` / `portfolio/`)関連** であること
  - 「時間枠を埋める」「キューを盛る」ことが目的化していないこと
  - long-run 中も §6(Architecture Astronaut 検知)が動作していること
  - 連続する Ops 系 PR が3本を超えていないこと
- 上記を満たさない場合、long-run は禁止。短時間 run + 人間レビューに切り替える。
- long-run 開始前に、キューの構成比(signals / ops / data / ui / その他の%)を提示し、ユーザー承認を得ること。

## §13. MD ファイル経由の応答(チャット UI 遅延対策)

- コマンド出力、長文の調査結果、テストレポート、PR diff、ファイル一覧、長い分析など、**チャット表示が重くなる出力はすべて MD ファイル化**する。
- チャット本文には以下のみ残す:
  - 結論(1〜3行)
  - 確度(%)
  - MD ファイルへのパス
  - 人間が判断すべき次のアクション(1行)
- MD ファイルは `reports/YYYY-MM-DD/` 配下に統一保存。
- ファイル先頭に **3行サマリー** を必ず置く(人間がファイルを開かずに済む場合があるため)。
- 例外:結論そのものが3行以下で済む場合は、MD ファイル化せずチャットに直接記載してよい。

## §14. テストレポートの一括コピー対応

- すべてのテストレポートは、人間がワンクリックでコピー&ペーストできる形で出力すること。
- 出力先:
  - 単一の fenced code block にまとめる、または
  - 専用 MD ファイル `reports/YYYY-MM-DD/test_report_<task_id>.md` として出力(§13 と統合)
- レポートには必ず以下を含む:
  - PASS / FAIL 件数
  - 失敗テストのファイル名と行番号
  - 再現手順(コマンド1行で再実行できる形)
  - 環境情報(Python バージョン、主要依存ライブラリ)
- レポート末尾に「人間がコピペすべき範囲」を明示マーカーで囲む:`<<< COPY FROM HERE >>>` 〜 `<<< COPY TO HERE >>>`

## §15. ターミナル作業の明示マーキング

- 人間がターミナルで作業する必要がある場合、応答冒頭に必ず以下を明記:
  - `[TERMINAL ACTION REQUIRED]`
  - 想定所要時間
  - 失敗時のフォールバック
- ターミナル作業が不要な場合は、応答冒頭に `[AGENT-ONLY]` を明記。
- 両者を1応答に混在させない。混在しそうな場合は応答を2つに分割し、それぞれを `[TERMINAL ACTION REQUIRED]` / `[AGENT-ONLY]` で明確に区切る。
- `[TERMINAL ACTION REQUIRED]` のコマンドは必ず1ブロックにまとめ、コピペで一括実行可能な形で提示。

## §16. 進捗率の表示(単一 % 禁止)

- コマンド指示の応答ごとに開発進捗を簡潔に表示する。
- **単一の総合 % は禁止**(合成誤謬、Goodhart's Law、§6 を助長するため)。
- 表示形式は以下のドメイン別とする:

```
[Progress]
  signals: XX%  (該当する 3 ファイル中 N ファイルが本番観察モードで稼働)
  risk:    XX%
  ops:     XX%
  data:    XX%
  ui:      XX%
  ---
  投資ロジック稼働までの残作業: N 件
```

- 「投資ロジック稼働までの残作業件数」は、§5 で禁止された Ops 機能追加を解除するための具体条件カウンタとして扱う。
- 進捗 % が前回より下がった場合(再見積もり)、その理由を1行で明示すること。
- 「overall: 73%」のような加重平均表記が出現したら §16 違反として停止し、ドメイン別表記に修正すること。

## §17. エージェントによるエラー対応の自動化

- ビルド失敗、テスト失敗、lint エラー、型エラー、import エラーなどの解決は原則エージェントが行い、人間に都度ターミナル作業をさせない。
- 自己解決した場合は §13 の MD ファイル形式で「**原因 → 修正 → 検証**」を必ず記録する。
- 以下のエラーは **エージェント自己解決禁止**、必ず人間に判断を仰ぐ:
  1. §1 で禁止された操作が要求されるエラー
  2. 投資ロジック(`signals/` / `risk/` / `portfolio/`)の挙動が変わるエラー
  3. セキュリティ・認証関連のエラー(API key、token、credentials、`.env`)
  4. データ正確性に関わるエラー(銘柄コード正規化、為替計算、税計算、日付処理)
  5. **「テストを期待値に合わせて変更する」型の修正**(根本原因隠蔽の典型パターン、絶対禁止)
  6. ライブラリのメジャーバージョン変更を伴うエラー
  7. CI / GitHub Actions / pyproject.toml に触れる必要があるエラー
- 同じエラーを 30 日以内に 2 回以上自己解決した場合、根本原因がコード以外(設計・依存・環境)にある可能性を警告し、人間レビューを要求する。
- エラー対応の所要時間が想定の3倍を超えた場合、自己解決を停止して人間に報告する(暴走防止)。

---

## §18. このルールへの追加履歴

- 2026-05-23: 初版作成(§0〜§11)
- 2026-05-23: §12〜§17 追加(長時間 run、MD ファイル化、テストレポート、ターミナルマーキング、進捗表示、エラー対応自動化)
- (以降、ルール追加・改訂はここに日付付きで記録)

---

**End of RULES.md**
