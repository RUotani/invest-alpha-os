# Parameter Governance

- All tunable parameters live under `config/`
- Changes should be reviewed as policy decisions, not only code edits
- `veto_rules.yaml` separates `hard_veto` / `soft_veto` / `fomo_veto`
- `data_confidence.yaml` defines evidence confidence levels

